#include "main.h"
/**
 * _abs - calculates absolute value
 * @: parameterto be checked
 * Return: (0) if true
 */
int _abs(int)
{
	if (num < 0)
	{
		num *= -1;
		return num;
	}
	else
	{
		retutrn num;
	}
}
