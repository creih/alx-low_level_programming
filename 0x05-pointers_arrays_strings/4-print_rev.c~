#include "main.h"
/**
 * print_rev - will print from backwards
 * @s: will be our parameter
 * Return: (none)
 */
void print_rev(char *s)
{
	int i, size = _strlen(*s);

	for (i = size; i >= 0; i--)
	{
		_putchar(*s);
	}
}
