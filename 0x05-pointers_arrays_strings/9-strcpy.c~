#include "main.h"
/**
 * *_strcpy -copies the pointer dest
 * @dest: final place
 * @src: source
 * Return: dest if true
 */
char *_strcpy(char *dest, char *src)
{
	int index = 0;

	while (src[index] != '\0')
	{
		dest[index] = src[index];
	}
	dest[index] = '\0';
	return (dest);
}
