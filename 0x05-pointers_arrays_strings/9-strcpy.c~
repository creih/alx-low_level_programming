#include "main.h"
/**
 * *_strcpy -copies the pointer dest
 * @dest: final place
 * @src: source
 * Return: dest if true
 */
char *_strcpy(char *dest, char *src)
{
	char *res = dest;
	while (*src)
	{
		*dest++ = *src++;
	}
	return (res);
}
