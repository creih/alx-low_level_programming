#include "main.h"
/**
 * _puts - will print all chrs of the string
 * @*str: will be the parameter of file
 * Return: none
 */
void _puts(char *str)
{
	while (*str != '\0')
	{
		_putchar(*str);
		str++;
	}
	_putchar('\n');
}
