#include "main.h"
/**
 * puts2 -prints 2nd chars
 * @str: parameter to use
 * Return: none
 */
void puts2(char *str)
{
	char *current;
	int index;

	for (index = 0; index <= _strlen(str); index += 2)
	{
		current = str[index];
		_putchar(*current);
	}
}
