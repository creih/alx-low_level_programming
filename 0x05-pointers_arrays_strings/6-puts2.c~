#include "main.h"
/**
 * puts2 -prints 2nd chars
 * @str: parameter to use
 * Return: none
 */
void puts2(char *str)
{
	int index;

	for (index = 0; index <= _strlen(str); index += 2)
	{
		_putchar(*str[index]);
	}
}
