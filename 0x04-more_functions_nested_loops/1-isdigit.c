#include "main.h"
/**
 * _isdigit - will check if c is num
 * @c: will becheked
 * Return: (1)true, (0)otherwise
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
